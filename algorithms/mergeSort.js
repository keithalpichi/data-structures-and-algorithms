/*
  Divide by two until single array then merge back up?
*/

const mergeSort = (array) => {
  if (array.length > 1) {
    // split array
    // call mergeSort on each splitted array
  }
  // At this point we assume the array length equals 1
  // We can now insert sort
}

console.log(mergeSort([4,7,9,1,3,8,2]))
